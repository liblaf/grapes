INHERIT: .config/copier/mkdocs.yaml
site_name: Grapes
site_description: 🍇 Supercharge your Python with rich logging, precise timing, and seamless serialization.

plugins:
  mkdocstrings:
    handlers:
      python:
        options:
          preload_modules:
            - environs # I don't know why, but this makes mkdocstrings work
